#include <stdio.h>
#include "variadic_functions.h"
#include <stdarg.h>

/**
 *print sum of all parameters
 *Return: 0
*/

int sum_them_all(const unsigned int n, ...);
 {
	 if(n == 0 )
	 return 0;
 }
int main (void)
{
	int n;
	n = sum of parameters;
	printf("%d /n",n);
	return sum;
}

